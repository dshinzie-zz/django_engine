from django.conf.urls import url
from django.contrib import admin
from . import views

urlpatterns = [
    #merchants
    # url(r'^api/v1/merchants/$', views.artist_list, name='artist_list'),
    # url(r'^artists/(?P<pk>\d+)/$', views.artist_detail, name='artist_detail'),
]
